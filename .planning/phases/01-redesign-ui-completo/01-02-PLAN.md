---
phase: 01-redesign-ui-completo
plan: 01-02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - components/layout/TopStrip.tsx
  - components/public/Header.tsx
  - components/layout/CategoryNavBar.tsx
  - components/layout/MobileBottomNav.tsx
  - components/layout/Footer.tsx
  - components/news/BreakingNewsBanner.tsx
  - app/(public)/layout.tsx
autonomous: true
requirements: [HDR-01, HDR-02, HDR-03, HDR-04, FTR-01, FTR-02, RES-01, RES-02]
goal: "Rebuild the site chrome (TopStrip, Header, CategoryNavBar, Footer, MobileBottomNav) to mirror the Stitch desktop + mobile-b contract, replacing the floating-card header and gradient-CTA footer."
must_haves:
  truths:
    - "Desktop visitors see a 3-row header stack: on-surface TopStrip (date/weather/social/red ANUNCIE AQUI pill) → white logo+search bar → primary-container sticky CategoryNavBar with 9 category links pulled from DB + amber ANUNCIE AQUI CTA."
    - "Mobile visitors see a compact sticky header (menu + italic Newsreader 'Foz em Foco' brand + amber 'Anuncie' button) and a fixed MobileBottomNav with 4 items (Notícias / Turismo / Guia / Empregos)."
    - "The footer is navy (bg-on-surface text-surface) with a 4-column editorial grid on desktop (Brand / Editorial / Institucional / Newsletter) that collapses to 1 column on mobile, plus a bottom bar with uppercase copyright."
    - "Search overlay logic is preserved from the old Header (hook/endpoint untouched), only the shell is restyled."
    - "BreakingNewsBanner renders only when there's an active breaking post; when rendered, it uses the new tokens (bg-secondary text-on-secondary) instead of legacy .breaking-banner class."
    - "app/(public)/layout.tsx composes the new shell: <TopStrip /> <Header /> <CategoryNavBar /> <BreakingNewsBanner /> <main>{children}</main> <Footer /> <MobileBottomNav />."
    - "Nav categories remain DB-driven (getCategories()) — no hardcoded IMÓVEIS/EMPREGOS labels."
  artifacts:
    - path: "components/layout/TopStrip.tsx"
      provides: "Desktop-only top strip (date, weather placeholder, social icons, red ANUNCIE AQUI pill)"
      contains: "ANUNCIE AQUI"
    - path: "components/public/Header.tsx"
      provides: "White logo+search+icons bar, search overlay preserved"
      contains: "font-headline"
    - path: "components/layout/CategoryNavBar.tsx"
      provides: "Sticky bg-primary-container nav with DB-driven categories + amber CTA"
      contains: "bg-primary-container"
    - path: "components/layout/MobileBottomNav.tsx"
      provides: "Fixed bottom nav for mobile, 4 items"
      contains: "md:hidden"
    - path: "components/layout/Footer.tsx"
      provides: "Navy 4-col footer"
      contains: "bg-on-surface"
    - path: "app/(public)/layout.tsx"
      provides: "New shell composition"
      contains: "CategoryNavBar"
  key_links:
    - from: "components/layout/CategoryNavBar.tsx"
      to: "lib queries getCategories"
      via: "server fetch of active categories passed as prop"
      pattern: "getCategories\\("
    - from: "components/public/Header.tsx"
      to: "existing search overlay hook/endpoint"
      via: "preserved client logic"
      pattern: "useSearch|/api/search"
    - from: "app/(public)/layout.tsx"
      to: "all new layout components"
      via: "JSX composition"
      pattern: "<CategoryNavBar"
---

<objective>
Replace the entire site chrome. The current Header is a single "floating card" with inline styles and a Bebas logo; the current Footer is a 4-col block wrapped in an amber gradient CTA card. Both diverge from Stitch.

This plan delivers 5 new/rewritten layout components (TopStrip, Header, CategoryNavBar, MobileBottomNav, Footer) and updates the public layout to compose them in the order Stitch demands. The search overlay from the current Header is preserved (logic only, shell restyled). BreakingNewsBanner is restyled with new tokens and kept as a conditional render (per user directive #5). Nav categories stay DB-driven (per user directive #2).

Output: a cohesive shell that any public page (home, category, post, search, /anunciantes) inherits from app/(public)/layout.tsx.
</objective>

<context_summary>
- RESEARCH §1.1 rows 1–4 (TopStrip, Header, Leaderboard, Navigation) and row 11 (Footer) give the desktop markup verbatim, including exact Tailwind classes, spacing, and typography.
- RESEARCH §1.3 section 1 (TopAppBar) and §1.3 section 9 (BottomNavBar) give the mobile-b header and bottom nav.
- RESEARCH §2.3 flags Header, Footer, BusinessDirectory, BreakingNewsBanner as FULL REWRITE.
- CONTEXT.md directive: planner decides icon mapping (lucide). RESEARCH §4 Plan 01-02 already lists the Material Symbols → lucide mapping.
- User directive #2: nav categories must remain DB-driven; only styling mirrors Stitch. User directive #5: BreakingNewsBanner stays conditional, restyled inside this plan.
</context_summary>

<pre_flight_reads>
Executor MUST read these files in full before any edit:
1. C:\Users\crist\foz-foco-real\.planning\phases\01-redesign-ui-completo\01-RESEARCH.md (sections 1.1 rows 1-4 + row 11, 1.3 sections 1 and 9, 2.3, 4 Plan 01-02)
2. C:\Users\crist\foz-foco-real\.planning\research\stitch\desktop.html (Row 1 top strip, Row 2 header, Row 4 nav, Row 11 footer — search for `bg-on-surface`, `bg-primary-container`, and the footer `grid-cols` markup)
3. C:\Users\crist\foz-foco-real\.planning\research\stitch\mobile-b.html (sections 1 TopAppBar and 9 BottomNavBar)
4. C:\Users\crist\foz-foco-real\components\public\Header.tsx (full file — 417 lines — to extract search overlay logic, theme toggle, settings fetch)
5. C:\Users\crist\foz-foco-real\components\layout\Footer.tsx
6. C:\Users\crist\foz-foco-real\components\news\BreakingNewsBanner.tsx
7. C:\Users\crist\foz-foco-real\app\(public)\layout.tsx
8. C:\Users\crist\foz-foco-real\lib\queries or equivalent — whichever file exposes `getCategories()` (grep for `getCategories` in repo)
9. C:\Users\crist\foz-foco-real\tailwind.config.ts (confirm plan 01-01 tokens are present)
10. C:\Users\crist\foz-foco-real\app\globals.css (confirm plan 01-01 slim rewrite is in place)
</pre_flight_reads>

<tasks>

<task type="auto" id="01-02-T1">
  <name>Task 1: Create TopStrip + rewrite Header + create CategoryNavBar</name>
  <files>components/layout/TopStrip.tsx, components/public/Header.tsx, components/layout/CategoryNavBar.tsx</files>
  <action>
Create `components/layout/TopStrip.tsx` (server component, desktop-only):
- Root: `<div class="hidden md:block bg-on-surface text-surface">`
- Inner: `<div class="max-w-[1200px] mx-auto h-8 px-4 flex items-center justify-between text-[11px] font-label tracking-wider uppercase font-medium">`
- LEFT flex: current date in pt-BR via date-fns (`format(new Date(), "EEEE, dd 'de' MMMM 'de' yyyy", { locale: ptBR })`) + separator `|` + `Foz do Iguaçu - PR` + separator + static weather placeholder (`<Sun className="w-3 h-3" /> 28°C`)
- RIGHT flex gap-4: social icons row (Globe, Share2, Mail — each `w-3 h-3`) + red pill `<Link href="/anunciantes" class="bg-secondary text-white px-3 py-1 rounded-full font-bold">ANUNCIE AQUI</Link>`
- Mirror desktop.html Row 1 verbatim. Use date-fns + lucide-react only (already in deps).

Rewrite `components/public/Header.tsx` (client component — keeps search overlay):
- Remove ALL inline `style={{}}` usage. Remove Bebas logo.
- Root: `<header class="bg-white h-20">` (no sticky — CategoryNavBar below is the sticky element, per Stitch Row 2 static / Row 4 sticky)
- Inner: `<div class="max-w-[1200px] h-full mx-auto px-4 flex items-center justify-between gap-8">`
- LEFT (logo block): `<Link href="/" class="flex flex-col items-start">`
  - `<h1 class="text-3xl font-black text-primary-container font-headline tracking-tighter leading-none">FOZ EM FOCO</h1>`
  - `<p class="text-[10px] font-label uppercase tracking-widest text-outline mt-1">O portal de notícias de Foz do Iguaçu</p>`
- CENTER (search bar): `<div class="flex-1 max-w-md mx-12 relative">`
  - `<input type="search" placeholder="Buscar no portal..." class="w-full bg-surface-container rounded-full px-6 py-2 text-sm font-body focus:outline-none focus:ring-2 focus:ring-primary-container" />`
  - `<Search class="absolute right-4 top-1/2 -translate-y-1/2 w-4 h-4 text-outline" />`
  - **Preserve existing search overlay logic**: whatever hook (useState + fetch to /api/search or equivalent) currently powers the overlay must still trigger when the input is focused/submitted. Port it verbatim; only the wrapping JSX changes. If the current Header uses a custom `useSearch` hook or local state, migrate that state into the rewritten component.
- RIGHT: theme toggle button (preserve existing ThemeToggle logic — use `next-themes`) + `<User class="w-5 h-5 text-primary-container" />` icon button + `<Menu class="w-5 h-5 text-primary-container md:hidden" />` mobile trigger
- Mobile variant (below md breakpoint, use `md:hidden` on desktop shell and a separate `md:hidden` block for mobile): follow RESEARCH §1.3 section 1 — sticky top-0 z-50 bg-surface/80 backdrop-blur-md shadow-sm h-16 px-6 flex justify-between; LEFT Menu button; CENTER `<h1 class="font-headline text-2xl font-bold italic text-primary-container">Foz em Foco</h1>`; RIGHT amber CTA `<Link href="/anunciantes" class="bg-tertiary-fixed text-on-tertiary-fixed text-xs px-4 py-2 font-bold uppercase tracking-wider rounded-sm">Anuncie</Link>`. Use a single component file with responsive wrappers (`hidden md:flex` for desktop block, `flex md:hidden` for mobile block).
- Preserve logo-from-settings fetch if the current Header pulls it from `/api/admin/settings`. If present, keep the fetch but default to the static "FOZ EM FOCO" wording until the settings payload arrives.

Create `components/layout/CategoryNavBar.tsx` (server component, receives `categories` prop from `getCategories()`):
- Signature: `export function CategoryNavBar({ categories }: { categories: Category[] })`
- Root: `<nav class="hidden md:block sticky top-0 z-50 bg-primary-container shadow-lg">`
- Inner: `<div class="max-w-[1200px] mx-auto flex items-center justify-between">`
- LEFT: `<ul class="flex items-stretch">` with one `<li>` per category. Each `<li>` renders `<Link href="/categoria/${cat.slug}" class="px-5 py-4 text-white font-label text-xs font-bold tracking-widest uppercase hover:bg-white/5">${cat.name}</Link>`. Prepend a static HOME link `<Link href="/" class="...">HOME</Link>`. DO NOT hardcode IMÓVEIS / EMPREGOS — if the DB has those categories, they appear; if not, they don't.
- RIGHT (inside the same max-w-1200 flex container): amber CTA `<Link href="/anunciantes" class="bg-tertiary-fixed text-on-tertiary-fixed px-4 py-2 rounded-sm text-xs font-bold uppercase tracking-widest m-2">ANUNCIE AQUI</Link>`
- Mirror desktop.html Row 4 markup and spacing verbatim.

Scope guard: these three files together reproduce Stitch desktop.html rows 1, 2, and 4 plus the mobile-b header from sections 1. Nothing else.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && npx tsc --noEmit 2>&1 | grep -E "(TopStrip|Header|CategoryNavBar)\\.tsx" ; test $? -ne 0</automated>
  </verify>
  <done>TopStrip, Header (rewritten), CategoryNavBar exist and compile. Search overlay logic preserved. No inline `style={{}}` in any of the three files. Nav consumes `categories` prop (DB-driven). Amber CTA present with `bg-tertiary-fixed text-on-tertiary-fixed`.</done>
</task>

<task type="auto" id="01-02-T2">
  <name>Task 2: Create MobileBottomNav + rewrite Footer + handle BreakingNewsBanner</name>
  <files>components/layout/MobileBottomNav.tsx, components/layout/Footer.tsx, components/news/BreakingNewsBanner.tsx</files>
  <action>
Create `components/layout/MobileBottomNav.tsx` (server component):
- Mirror mobile-b.html section 9.
- Root: `<nav class="fixed bottom-0 left-0 right-0 z-50 md:hidden bg-white/90 backdrop-blur-xl shadow border-t border-outline-variant py-3 pb-safe flex justify-around">`
- 4 items, each `<Link>` with a lucide icon + label underneath:
  1. Notícias (Newspaper, href `/`, active style `bg-blue-50 text-primary-container rounded-xl px-3 py-1` — but evaluate `usePathname()` to toggle active state; if this requires client, mark `'use client'`)
  2. Turismo (Compass, href `/categoria/turismo`)
  3. Guia (Briefcase or Store — pick Store from lucide, href `/categoria/guia-comercial`)
  4. Empregos (Briefcase, href `/categoria/empregos`)
- Each item: `<div class="flex flex-col items-center text-[10px] font-semibold uppercase tracking-wider text-outline">` with icon `w-5 h-5` above label.
- Notícias is the active item when pathname is `/`. Use `next/navigation` `usePathname()` (requires `'use client'`).
- Add `pb-safe` via inline style for iOS safe-area: `style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}`.

Rewrite `components/layout/Footer.tsx` (server component):
- Mirror desktop.html Row 11 verbatim.
- Root: `<footer class="bg-on-surface text-surface py-20 px-4">`
- Inner: `<div class="max-w-[1200px] mx-auto grid grid-cols-1 md:grid-cols-4 gap-12">`
- Col 1 — Brand:
  - `<h2 class="text-2xl font-black font-headline text-on-primary-container tracking-tighter">FOZ EM FOCO</h2>`
  - `<p class="text-sm text-outline-variant mt-2">O portal de notícias de Foz do Iguaçu</p>`
  - Social icons row (Facebook, Instagram, Youtube from lucide) — each `w-4 h-4`, wrapped in `<a>` tags with hover `hover:text-on-primary-container`.
- Col 2 — Editorial:
  - Heading `<h3 class="font-bold text-xs uppercase tracking-widest text-on-primary-container mb-4">Editorial</h3>`
  - `<ul class="space-y-3 text-sm text-outline-variant">` with links: Cidades, Política, Economia, Esporte, Cultura (use `/categoria/{slug}` hrefs; static list acceptable since footer nav labels are editorial choices not strictly DB-bound).
- Col 3 — Institucional:
  - Same pattern, links: Sobre Nós, Expediente, Anuncie (→ `/anunciantes`), Privacidade, Contato.
- Col 4 — Newsletter:
  - Heading same style: "Newsletter"
  - Paragraph `text-sm text-outline-variant`: "Receba as principais notícias de Foz direto no seu e-mail."
  - Form (static — `action="#" method="post"`; or wire to existing newsletter endpoint if one exists in `/api/newsletter` — grep to confirm, else static):
    - `<input type="email" placeholder="Seu e-mail" class="bg-white/5 border border-white/10 rounded-md px-3 py-2 text-sm w-full" />`
    - `<button class="mt-3 bg-on-primary-container text-on-primary-fixed px-4 py-2 rounded-md text-xs font-bold uppercase tracking-widest w-full">Inscrever-se</button>`
- Bottom bar (outside the 4-col grid, still inside footer):
  - `<div class="max-w-[1200px] mx-auto mt-20 pt-8 border-t border-white/10 flex justify-between text-[10px] uppercase tracking-widest text-outline-variant">`
  - LEFT: `© ${new Date().getFullYear()} Foz em Foco. Todos os direitos reservados.`
  - RIGHT: `Feito em Foz do Iguaçu`

DO NOT add the amber gradient CTA card that was in the old footer — per Stitch, that belongs to the Pricing section (plan 01-03) and NOT the footer.

Rewrite `components/news/BreakingNewsBanner.tsx`:
- Keep the conditional render logic (only renders when an active breaking post exists — preserve the existing data fetch / prop contract).
- Restyle the shell: replace any `.breaking-banner` class with Tailwind utilities:
  - Root: `<div class="w-full bg-secondary text-on-secondary py-2 px-4">`
  - Inner: `<div class="max-w-[1200px] mx-auto flex items-center gap-3 text-xs font-label font-bold uppercase tracking-widest">`
  - LEFT: pill `<span class="bg-white text-secondary px-2 py-0.5 rounded-full">URGENTE</span>`
  - CENTER: `<Link href={post.slug} class="truncate hover:underline">{post.title}</Link>`
- No animation — keep it static (Stitch has no breaking banner so this is a concession to existing data; keep it subtle).
- If the component currently takes no props and fetches internally, leave that logic alone; just swap the JSX shell.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && grep -E "breaking-banner|style=\\{\\{.*gradient" components/layout/Footer.tsx components/news/BreakingNewsBanner.tsx ; test $? -ne 0</automated>
  </verify>
  <done>MobileBottomNav renders on mobile only, Footer is navy 4-col with no gradient CTA card, BreakingNewsBanner is restyled with bg-secondary + conditional render preserved. All three files compile.</done>
</task>

<task type="auto" id="01-02-T3">
  <name>Task 3: Compose new shell in app/(public)/layout.tsx + build verification</name>
  <files>app/(public)/layout.tsx</files>
  <action>
Rewrite `app/(public)/layout.tsx` to compose the new chrome in the exact Stitch order. This is a server component — `getCategories()` is called server-side and passed to `CategoryNavBar`.

Target structure:
```tsx
import { TopStrip } from '@/components/layout/TopStrip'
import { Header } from '@/components/public/Header'
import { CategoryNavBar } from '@/components/layout/CategoryNavBar'
import { BreakingNewsBanner } from '@/components/news/BreakingNewsBanner'
import { Footer } from '@/components/layout/Footer'
import { MobileBottomNav } from '@/components/layout/MobileBottomNav'
import { getCategories } from '@/lib/queries' // or wherever it lives

export default async function PublicLayout({ children }: { children: React.ReactNode }) {
  const categories = await getCategories()
  return (
    <div className="min-h-screen flex flex-col bg-surface text-on-surface font-body">
      <TopStrip />
      <Header />
      <CategoryNavBar categories={categories} />
      <BreakingNewsBanner />
      <main className="flex-1 pb-24 md:pb-0">{children}</main>
      <Footer />
      <MobileBottomNav />
    </div>
  )
}
```

Notes:
- `pb-24 md:pb-0` on `<main>` reserves space for the fixed MobileBottomNav on mobile.
- `getCategories()` import path: grep the repo for the current export. If it lives in `lib/queries.ts` or `lib/data.ts` or similar, use that. Do NOT rewrite the query — it is backend-safe per scope.
- Delete any old chrome wrappers (e.g., `.page-layout` div) from the previous layout.tsx.
- Preserve any Suspense boundary or providers the old layout had (ThemeProvider is in root layout.tsx, not here — confirm before removing).

Run a full build:
```bash
cd C:/Users/crist/foz-foco-real
export DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder"
npm run build 2>&1 | tee .build.log
```
Expected: exits 0. If a component is missing an import or a prop, fix before committing.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder" npm run build 2>&1 | grep -q "Compiled successfully"</automated>
  </verify>
  <done>app/(public)/layout.tsx composes TopStrip + Header + CategoryNavBar + BreakingNewsBanner + main + Footer + MobileBottomNav in that order. Categories are fetched server-side and passed to CategoryNavBar. Build passes.</done>
</task>

</tasks>

<files_created>
- components/layout/TopStrip.tsx
- components/layout/CategoryNavBar.tsx
- components/layout/MobileBottomNav.tsx
</files_created>

<files_modified>
- components/public/Header.tsx (FULL REWRITE — search logic preserved)
- components/layout/Footer.tsx (FULL REWRITE — navy 4-col, no gradient CTA)
- components/news/BreakingNewsBanner.tsx (shell restyle — conditional render preserved)
- app/(public)/layout.tsx (shell composition)
</files_modified>

<files_deleted>
- (none)
</files_deleted>

<acceptance_criteria>
1. Vercel preview `/` desktop view shows 3 stacked rows at top: navy strip (date + ANUNCIE AQUI red pill) → white logo bar (FOZ EM FOCO + search) → navy sticky category nav with amber CTA.
2. Vercel preview `/` mobile view shows: compact sticky header (menu + italic logo + amber Anuncie) and a fixed bottom nav with 4 items. TopStrip and CategoryNavBar hidden.
3. Footer on desktop: 4 columns (Brand / Editorial / Institucional / Newsletter), navy background, bottom bar with copyright. On mobile: single stacked column.
4. Search input in header remains functional (typing triggers whatever the current overlay logic does — SWR / fetch / dropdown).
5. Category nav labels reflect the actual DB categories (not hardcoded IMÓVEIS/EMPREGOS from Stitch — verified by removing a category from DB and seeing it disappear from nav).
6. Dark mode: all chrome renders correctly — navy becomes lighter or stays navy with surface text; amber CTA remains visible; no white-on-white.
7. BreakingNewsBanner renders only when a breaking post is active; when visible, it's a thin red bar with URGENTE pill + title.
8. `npm run build` exits 0; Vercel preview READY.
</acceptance_criteria>

<verification_steps>
1. Local build with placeholder DATABASE_URL — must succeed.
2. Push → Vercel preview.
3. Desktop check:
   - Top strip visible with today's date in Portuguese.
   - Search bar in header accepts input, overlay triggers.
   - Category nav sticky on scroll.
   - Amber "ANUNCIE AQUI" button visible in nav bar.
4. Mobile check (Chrome DevTools responsive 375×812):
   - TopStrip hidden.
   - Header shows compact version.
   - MobileBottomNav fixed at bottom with 4 items.
   - No horizontal scroll.
5. Footer check:
   - Desktop: 4 columns visible.
   - Mobile: stacked single column.
   - Social icons in Brand column.
   - Newsletter form in col 4.
6. Dark mode toggle: all chrome elements still legible and on-brand.
7. Test a category without Stitch name (e.g., `cidade`, `saude`) appears in the nav bar — confirms DB-driven.
</verification_steps>

<success_criteria>
- All 3 tasks green.
- Build passes locally + Vercel READY.
- All 5 new/rewritten components mount correctly in app/(public)/layout.tsx.
- Category nav is DB-driven (verified by inspecting rendered HTML).
- Search overlay logic from old Header survives the rewrite.
</success_criteria>

<commit_message>
feat(01-02): rebuild site chrome (TopStrip, Header, CategoryNavBar, Footer, MobileBottomNav)

- components/layout/TopStrip.tsx: NEW desktop-only strip with date/weather/social/red ANUNCIE AQUI pill (mirrors desktop.html Row 1)
- components/public/Header.tsx: FULL REWRITE — white logo+search bar (mirrors desktop.html Row 2), mobile compact variant (mirrors mobile-b section 1), search overlay logic preserved
- components/layout/CategoryNavBar.tsx: NEW sticky bg-primary-container nav, DB-driven categories (getCategories) + amber ANUNCIE AQUI CTA (mirrors desktop.html Row 4)
- components/layout/MobileBottomNav.tsx: NEW fixed bottom nav, 4 items (Notícias/Turismo/Guia/Empregos), md:hidden (mirrors mobile-b section 9)
- components/layout/Footer.tsx: FULL REWRITE — navy bg-on-surface, 4-col editorial grid, no gradient CTA card (mirrors desktop.html Row 11)
- components/news/BreakingNewsBanner.tsx: restyle shell with bg-secondary, keep conditional render
- app/(public)/layout.tsx: compose new chrome order (TopStrip → Header → CategoryNavBar → BreakingNewsBanner → main → Footer → MobileBottomNav)

Nav categories stay DB-driven. Search overlay logic preserved. Depends on 01-01 tokens.
</commit_message>

<dependencies>
- Depends on: 01-01 (tokens, fonts, dark-mode HSL)
- Blocks: 01-03 (homepage uses the new shell for context), 01-04 (landing /anunciantes also uses the new shell)
</dependencies>

<output>
After completion, create `.planning/phases/01-redesign-ui-completo/01-02-SUMMARY.md` documenting:
- Final JSX composition of app/(public)/layout.tsx
- Lucide icon mapping used (Material Symbol → lucide component)
- Search overlay logic migration notes (what was kept from old Header)
- Newsletter form wiring decision (static vs endpoint)
- Dark-mode verification notes
</output>
