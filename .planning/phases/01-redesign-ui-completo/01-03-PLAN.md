---
phase: 01-redesign-ui-completo
plan: 01-03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - app/(public)/page.tsx
  - components/news/HeroSection.tsx
  - components/news/ArticleCard.tsx
  - components/news/CategoryBadge.tsx
  - components/sections/MainGrid.tsx
  - components/sections/CategorySection.tsx
  - components/layout/Sidebar.tsx
  - components/widgets/TrendingWidget.tsx
  - components/widgets/UltimasList.tsx
  - components/widgets/NewsletterWidget.tsx
  - components/sections/GuiaComercialSection.tsx
  - components/sections/PricingSection.tsx
  - components/sections/EventsScroller.tsx
  - components/sections/JobsSection.tsx
  - components/ads/BusinessDirectory.tsx
  - components/ads/BusinessCard.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, HOME-06, HOME-07, HOME-08, RES-01, RES-02]
goal: "Rebuild the homepage (hero + 3-col grid + sidebar widgets + Guia Comercial + placeholder Events + placeholder Jobs + Pricing section) to mirror the Stitch desktop and mobile-b contracts."
must_haves:
  truths:
    - "Desktop /: visitor sees a 12-col grid with col-span-8 main column (hero article + 3-col news sub-grid + inline 300×250 ad) and col-span-4 sidebar (halfpage ad placeholder + Mais Lidas numbered widget + rectangle ad + Últimas linear list)."
    - "Desktop /: below the main grid, full-width Guia Comercial section (6-col icon tiles), placeholder Events scroller, placeholder Jobs section, and Pricing section (3 tiers on navy gradient)."
    - "Mobile /: single-column stack — hero, 3 secondary cards, inline ad, Mais Lidas card, Guia Comercial (2-col tiles), Pricing (stacked 3 tiers), footer (chrome already provided by 01-02)."
    - "Grid cards show only image + uppercase category eyebrow + Newsreader title — NO excerpt, NO byline (ultra clean Stitch style)."
    - "Mais Lidas widget renders 3 items with italic Newsreader numbers 01/02/03 and border-b-4 border-primary heading treatment."
    - "Últimas list renders 3-5 items as HH:MM prefix + title with divide-y."
    - "getHomeData server fetch is untouched — only the JSX composition of app/(public)/page.tsx and the section components change."
    - "Events and Jobs sections render as static placeholders with TODO comments for future data wiring (user directive #3)."
    - "Pricing section on home uses R$ 299 / R$ 699 / R$ 1.999 (matching the existing /anunciantes values per RESEARCH §5 Q7) with Prata as the 'popular' scaled tier."
  artifacts:
    - path: "app/(public)/page.tsx"
      provides: "Homepage JSX composition (12-col grid + full-width sections)"
      contains: "grid-cols-12"
    - path: "components/news/HeroSection.tsx"
      provides: "HeroArticle — inline grid hero with gradient overlay"
      contains: "aspect-[16/9]"
    - path: "components/news/ArticleCard.tsx"
      provides: "Simplified variants: grid / secondary / compact"
      contains: "font-headline"
    - path: "components/layout/Sidebar.tsx"
      provides: "4-block sidebar (halfpage ad / MaisLidas / rectangle ad / UltimasList)"
      contains: "MaisLidas"
    - path: "components/sections/GuiaComercialSection.tsx"
      provides: "Full-width icon tile grid"
      contains: "grid-cols-2 md:grid-cols-6"
    - path: "components/sections/PricingSection.tsx"
      provides: "3-tier pricing on navy gradient (home variant)"
      contains: "MAIS POPULAR"
  key_links:
    - from: "app/(public)/page.tsx"
      to: "getHomeData (existing)"
      via: "server fetch returning allPosts, categories, sponsoredAd, trendingPosts"
      pattern: "getHomeData"
    - from: "components/layout/Sidebar.tsx"
      to: "trendingPosts prop"
      via: "MaisLidas consumes top 3 from trending"
      pattern: "trendingPosts"
    - from: "components/sections/PricingSection.tsx"
      to: "/anunciantes"
      via: "CTAs link to landing page"
      pattern: "/anunciantes"
---

<objective>
Rebuild `app/(public)/page.tsx` and every section/widget it composes to mirror the Stitch desktop main content and the mobile-b stacked layout. The backend query `getHomeData` is untouched.

Key structural change: current homepage uses a 4-col grid with `.main-grid` class and a sticky sidebar aside; the rewrite uses a 12-col grid (`col-span-8` main + `col-span-4` sidebar) on desktop and collapses to a single column on mobile. Cards are stripped of excerpt/byline to match the editorial minimalism of Stitch. Guia Comercial becomes a standalone full-width section with icon tiles. Events and Jobs are rendered as static placeholders (user directive #3). The Pricing section is new on the homepage and will be reused on `/anunciantes` in plan 01-04.

Output: a homepage that, on desktop, looks structurally identical to `desktop.html` rows 5–9 and on mobile collapses cleanly into the mobile-b stacking order.
</objective>

<context_summary>
- RESEARCH §1.1 rows 5–9 give the desktop main content, sidebar, Guia Comercial, Events, Jobs, and Pricing markup verbatim.
- RESEARCH §1.3 sections 3–8 give the mobile-b hero, secondary news, patrocinado, Guia, Pricing markup.
- RESEARCH §1.7 section inventory confirms which sections belong on homepage.
- RESEARCH §2.2 confirms `getHomeData` already provides allPosts, categories, sponsoredAd, trendingPosts — no new queries needed.
- RESEARCH §4 Plan 01-03 lists every file to touch and the target composition.
- User directive #3: Events and Jobs sections from Stitch desktop → render as static placeholders with TODO comments.
- User directive #5: BreakingNewsBanner already handled in 01-02, no homepage touch needed.
</context_summary>

<pre_flight_reads>
Executor MUST read these files in full before any edit:
1. C:\Users\crist\foz-foco-real\.planning\phases\01-redesign-ui-completo\01-RESEARCH.md (sections 1.1 rows 5-9, 1.3 sections 3-8, 1.5, 1.6, 2.2, 2.3, 4 Plan 01-03, 5 Q6 + Q7)
2. C:\Users\crist\foz-foco-real\.planning\research\stitch\desktop.html (rows 5-9 — main content, sidebar, Guia, Events, Empregos, Pricing)
3. C:\Users\crist\foz-foco-real\.planning\research\stitch\mobile-b.html (sections 3-8 — hero, secondary, patrocinado, guia, pricing)
4. C:\Users\crist\foz-foco-real\app\(public)\page.tsx (current JSX + getHomeData import path)
5. C:\Users\crist\foz-foco-real\components\news\HeroSection.tsx (current)
6. C:\Users\crist\foz-foco-real\components\news\ArticleCard.tsx (current — 4 variants to simplify)
7. C:\Users\crist\foz-foco-real\components\news\CategoryBadge.tsx
8. C:\Users\crist\foz-foco-real\components\sections\MainGrid.tsx
9. C:\Users\crist\foz-foco-real\components\sections\CategorySection.tsx
10. C:\Users\crist\foz-foco-real\components\layout\Sidebar.tsx
11. C:\Users\crist\foz-foco-real\components\widgets\TrendingWidget.tsx
12. C:\Users\crist\foz-foco-real\components\widgets\NewsletterWidget.tsx
13. C:\Users\crist\foz-foco-real\components\ads\BusinessDirectory.tsx
14. C:\Users\crist\foz-foco-real\components\ads\BusinessCard.tsx
15. C:\Users\crist\foz-foco-real\types\index.ts (Post / Category / Ad contracts)
</pre_flight_reads>

<tasks>

<task type="auto" id="01-03-T1">
  <name>Task 1: Rewrite HeroSection + ArticleCard + CategoryBadge + delete legacy sections</name>
  <files>components/news/HeroSection.tsx, components/news/ArticleCard.tsx, components/news/CategoryBadge.tsx, components/sections/MainGrid.tsx, components/sections/CategorySection.tsx, components/widgets/NewsletterWidget.tsx</files>
  <action>
**HeroSection.tsx** — rename exported component to `HeroArticle` (or keep `HeroSection` as an alias export). REWRITE contents:
- Props: `{ post: Post }` (reuse existing Post type)
- Root: `<Link href={`/${post.slug}`} class="relative block aspect-[16/9] overflow-hidden rounded-lg group">`
- Image: `<Image src={post.coverImage ?? post.media[0]?.url} alt={post.title} fill class="object-cover group-hover:scale-105 transition-transform duration-500" />`
- Gradient overlay: `<div class="absolute inset-0 bg-gradient-to-t from-on-surface via-on-surface/60 to-transparent" />`
- Content block absolute bottom-0 left-0 right-0 p-8 text-white:
  - Badge: `<CategoryBadge category={post.category} />`
  - Title: `<h2 class="text-4xl font-headline font-bold leading-tight mt-3">{post.title}</h2>`
  - Meta: `<p class="text-sm opacity-80 font-label mt-2">Por {post.author.name} • {formatRelativeDate(post.publishedAt)}</p>`
  - Excerpt: `<p class="text-lg font-light text-surface-container-high mt-3 line-clamp-2">{post.summary}</p>`
  - CTA line: `<span class="inline-block font-bold text-tertiary-fixed mt-4">Leia mais →</span>`
- Mirror desktop.html Row 5 hero article markup verbatim. Delete any `.hero-section` class usage.

**ArticleCard.tsx** — SIMPLIFY to 3 variants:
- Props: `{ post: Post; variant?: 'grid' | 'secondary' | 'compact' }` (default `grid`)
- `variant="grid"` (desktop 3-col sub-grid, mobile stacked):
  - `<Link href={`/${post.slug}`} class="group block space-y-3">`
  - `<div class="relative aspect-video overflow-hidden rounded-md">` with next/image
  - `<p class="text-[10px] font-bold text-primary tracking-widest uppercase">{post.category.name}</p>`
  - `<h3 class="font-headline font-bold text-lg leading-tight">{post.title}</h3>`
  - NO excerpt, NO byline.
- `variant="secondary"` (mobile-b style — text left, thumb right):
  - `<Link class="flex gap-4 items-start">`
  - LEFT: `<div class="flex-1"><p class="text-primary text-[10px] font-bold tracking-widest uppercase">{cat}</p><h3 class="font-headline text-xl font-bold leading-snug">{title}</h3></div>`
  - RIGHT: `<div class="w-24 h-24 shrink-0 overflow-hidden rounded-md"><Image ... /></div>`
- `variant="compact"` (sidebar Últimas-style, tiny):
  - `<Link class="flex gap-3 items-center py-2">`
  - thumb `w-16 h-16`, text `text-sm font-headline font-semibold leading-tight`
- Delete the old 4 variants (`featured`, `standard`, `compact`, `horizontal`). Delete all inline `style={{}}`.

**CategoryBadge.tsx** — ADJUST only:
- Default style: `<span class="inline-block bg-tertiary-fixed text-on-tertiary-fixed text-[10px] font-bold tracking-widest uppercase px-2 py-1 rounded-sm">{category.name}</span>`
- Optional `override` prop to use `category.color` as bg — keep the hook for legacy callers, but default is the amber treatment.

**MainGrid.tsx** — DELETE file (or keep as re-export of `NewsGrid3`). Since the new composition inlines the sub-grid inside `page.tsx`, a standalone component is not strictly needed. Decision: CREATE `NewsGrid3.tsx` as a tiny helper that takes `posts: Post[]` and renders `<div class="grid grid-cols-1 md:grid-cols-3 gap-6">` with 3 `<ArticleCard variant="grid">`. Delete or empty MainGrid.tsx to avoid duplicate imports. Update any import sites outside homepage (if any — grep `from '@/components/sections/MainGrid'`) to use NewsGrid3 or be removed.

**CategorySection.tsx** — DELETE file contents (replace with a no-op default export or delete the file entirely). Not in Stitch. Remove any imports from `app/(public)/page.tsx` in Task 4.

**NewsletterWidget.tsx** — DELETE file. Newsletter lives in Footer (plan 01-02). Remove any sidebar imports in Task 3.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && grep -E "(hero-section|article-card--featured|article-card--horizontal|style=\\{\\{.*color)" components/news/HeroSection.tsx components/news/ArticleCard.tsx components/news/CategoryBadge.tsx ; test $? -ne 0</automated>
  </verify>
  <done>HeroSection exports HeroArticle with inline grid hero markup. ArticleCard has 3 variants (grid/secondary/compact). CategoryBadge defaults to tertiary-fixed amber. MainGrid deleted or reduced to NewsGrid3 helper. CategorySection and NewsletterWidget deleted.</done>
</task>

<task type="auto" id="01-03-T2">
  <name>Task 2: Rewrite Sidebar + MaisLidas widget + create UltimasList widget</name>
  <files>components/layout/Sidebar.tsx, components/widgets/TrendingWidget.tsx, components/widgets/UltimasList.tsx</files>
  <action>
**Sidebar.tsx** — REWRITE as a 4-block stack:
- Props: `{ trendingPosts: Post[]; latestPosts: Post[] }`
- Root: `<aside class="space-y-12">`
- Block 1: `<AdSlot format="halfpage" position="SIDEBAR" />` — **NOTE: AdSlot is created in plan 01-04. For now, render a placeholder div** that matches the eventual AdSlot markup so this plan can ship without blocking:
  ```tsx
  <div class="w-[300px] h-[600px] mx-auto bg-surface-container border-2 border-dashed border-outline-variant flex items-center justify-center relative">
    <span class="absolute top-1 left-2 text-[8px] font-bold bg-tertiary-fixed text-on-tertiary-fixed px-1 rounded-sm">PUBLICIDADE</span>
    <span class="text-outline text-xs italic font-label">[ 300×600 — Espaço disponível ]</span>
  </div>
  ```
  Add a comment `// TODO(01-04): replace with <AdSlot format="halfpage" position="SIDEBAR" />`
- Block 2: `<MaisLidas posts={trendingPosts.slice(0, 3)} />`
- Block 3: rectangle ad placeholder (same shape as block 1 but `w-[300px] h-[250px]`, inline TODO)
- Block 4: `<UltimasList posts={latestPosts.slice(0, 5)} />`
- Remove all imports of NewsletterWidget, remove sticky behavior (Stitch sidebar is not sticky), remove inline styles.
- Hide entire sidebar on mobile via `class="hidden lg:block space-y-12"` — on mobile, sidebar blocks are inlined into the main stack separately (handled in Task 4 page composition).

**TrendingWidget.tsx → MaisLidas** — rename file to `MaisLidas.tsx` (or create new `MaisLidas.tsx` and delete the old file). Export `MaisLidas`:
- Props: `{ posts: Post[] }`
- Root: `<section class="space-y-4">`
- Header: `<h3 class="border-b-4 border-primary inline-block pr-8 pb-2 text-xl font-headline font-bold uppercase tracking-tight">Mais Lidas</h3>`
- List `<ul class="space-y-6 pt-4">`, each item:
  ```tsx
  <li class="flex gap-4 items-start">
    <span class="text-3xl font-black text-surface-variant font-headline italic shrink-0">{String(idx + 1).padStart(2, '0')}</span>
    <div class="flex-1">
      <Link href={`/${post.slug}`} class="font-headline font-bold leading-tight hover:text-primary">{post.title}</Link>
      <p class="text-[10px] text-outline font-medium mt-1">{post.views.toLocaleString('pt-BR')} visualizações</p>
    </div>
  </li>
  ```
- Mirror desktop.html Row 5 right column "Mais Lidas" block.

**UltimasList.tsx** — NEW file:
- Props: `{ posts: Post[] }`
- Root: `<section class="space-y-4">`
- Header: `<h3 class="border-b-4 border-secondary inline-block pr-8 pb-2 text-xl font-headline font-bold uppercase tracking-tight">Últimas</h3>`
- List `<ul class="divide-y divide-outline-variant/20">`, each item:
  ```tsx
  <li class="py-3">
    <Link href={`/${post.slug}`} class="text-sm font-medium font-body hover:text-primary block">
      <time class="text-outline font-bold mr-2">{format(post.publishedAt, 'HH:mm')}</time>
      — {post.title}
    </Link>
  </li>
  ```
- Use `date-fns` `format` with pt-BR locale. `publishedAt` comes in as a Date or ISO string — handle both.
- Mirror desktop.html Row 5 right column "Últimas" block.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && npx tsc --noEmit 2>&1 | grep -E "(Sidebar|MaisLidas|UltimasList)\\.tsx" ; test $? -ne 0</automated>
  </verify>
  <done>Sidebar stacks 4 blocks (halfpage placeholder / MaisLidas / rectangle placeholder / UltimasList), hidden on mobile. MaisLidas renders 3 numbered items with italic Newsreader digits and border-b-4 border-primary heading. UltimasList renders divide-y list with HH:MM prefix. NewsletterWidget no longer imported.</done>
</task>

<task type="auto" id="01-03-T3">
  <name>Task 3: Create GuiaComercialSection + PricingSection + EventsScroller + JobsSection placeholders + rewrite BusinessDirectory/BusinessCard</name>
  <files>components/sections/GuiaComercialSection.tsx, components/sections/PricingSection.tsx, components/sections/EventsScroller.tsx, components/sections/JobsSection.tsx, components/ads/BusinessDirectory.tsx, components/ads/BusinessCard.tsx</files>
  <action>
**GuiaComercialSection.tsx** — NEW. Full-width section mirroring desktop.html Row 6 + mobile-b section 6:
- Root: `<section class="bg-surface-container py-16">`
- Inner: `<div class="max-w-[1200px] mx-auto px-4">`
- Header: `<div class="flex justify-between items-end mb-10">` with `<h2 class="text-3xl font-headline font-bold text-on-surface">Guia Comercial</h2>` + `<Link href="/categoria/guia-comercial" class="text-primary font-bold text-sm">Ver todos →</Link>`
- Grid: `<div class="grid grid-cols-2 md:grid-cols-6 gap-4">`
- 6 static tiles (or pull from BusinessDirectory data if it exists — grep to confirm). Each tile:
  ```tsx
  <div class="bg-white p-4 md:h-[120px] rounded-md shadow-sm flex flex-col items-center justify-center gap-2 aspect-square md:aspect-auto">
    <Icon class="w-6 h-6 text-primary" />
    <span class="font-bold text-xs uppercase tracking-wider text-center">{label}</span>
  </div>
  ```
  Suggested static tiles: Restaurantes (UtensilsCrossed), Hotéis (Bed), Compras (ShoppingBag), Saúde (HeartPulse), Serviços (Wrench), Cadastre-se (PlusCircle — last tile with dashed border variant: `bg-on-tertiary-container/10 border-2 border-dashed border-on-tertiary-container/30`).

**PricingSection.tsx** — NEW. Reusable on home and /anunciantes via variant prop:
- Props: `{ variant?: 'home' | 'landing' }` (default `home`)
- Root: `<section class="py-24 bg-gradient-to-br from-primary via-primary-container to-on-surface text-white">`
- Inner: `<div class="max-w-[1200px] mx-auto px-4">`
- Header: center-aligned, `<h2 class="text-4xl font-headline font-bold text-center">Anuncie no Foz em Foco</h2>` + `<p class="text-center text-on-primary-container mt-3">Três planos para cada tamanho de negócio</p>`
- Grid: `<div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-12">` with 3 tier cards:
  - **Bronze**: `<div class="bg-white/10 backdrop-blur-md p-10 rounded-xl border border-white/20 text-center flex flex-col">` — title "Bronze" `text-xl font-bold`, price `R$ 299/mês` `text-3xl font-headline font-black mt-4`, features list (4-5 items with Check icon text-green-400), button `<Link href="/anunciantes" class="mt-auto border border-white/40 rounded-md py-3 font-bold text-xs uppercase tracking-widest">Contratar</Link>`
  - **Prata (popular)**: `<div class="bg-white text-on-surface p-10 rounded-xl shadow-2xl transform scale-105 z-10 relative flex flex-col">` — popular badge `<span class="absolute -top-4 left-1/2 -translate-x-1/2 bg-secondary text-white text-[10px] font-bold px-4 py-1 rounded-full uppercase tracking-widest">MAIS POPULAR</span>`, title "Prata" `text-xl font-bold text-primary`, price `R$ 699/mês` `text-3xl font-headline font-black text-primary mt-4`, features with `text-primary` Check icons, button `bg-primary text-white`
  - **Ouro**: same shell as Bronze, title "Ouro", price `R$ 1.999/mês`, features list, button ghost style
- Prices align with /anunciantes existing values per RESEARCH §5 Q7.
- Footer link below grid: `<a href="https://wa.me/5545999999999" class="block text-center mt-8 text-tertiary-fixed-dim hover:underline text-sm">Falar com um consultor via WhatsApp →</a>`

**EventsScroller.tsx** — NEW static placeholder (user directive #3):
- Root: `<section class="py-16 bg-white">`
- Inner max-w-1200 with header `<h2 class="text-3xl font-headline font-bold mb-10 px-4">Agenda de Eventos</h2>`
- Horizontal scroller: `<div class="flex gap-6 overflow-x-auto pb-8 px-4 scrollbar-hide">`
- 4 static placeholder cards, each `flex-none w-[300px]`:
  - Image placeholder `<div class="h-[400px] rounded-xl bg-surface-container-high" />` with absolute date badge `<div class="absolute top-4 left-4 bg-white/90 backdrop-blur p-2 rounded min-w-[50px] text-center"><span class="text-xl font-black block">15</span><span class="text-[10px] uppercase text-primary">MAR</span></div>`
  - Below: `<h3 class="font-headline font-bold text-xl mt-3">Nome do Evento</h3><p class="text-sm text-outline">Local do evento</p>`
- Add comment at top of file: `// TODO(future): wire to events DB model when available. Currently static placeholder per plan 01-03.`
- Mirror desktop.html Row 7.

**JobsSection.tsx** — NEW static placeholder:
- Root: `<section class="py-16 bg-surface-container-low">`
- Inner: max-w-1200 grid `<div class="max-w-[1200px] mx-auto px-4 grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">`
- LEFT `lg:col-span-4`: `<h2 class="text-4xl font-headline font-bold text-primary">Empregos em Foz</h2>` + paragraph + button `bg-primary text-white px-8 py-3 rounded-md font-bold tracking-widest text-xs uppercase`
- RIGHT `lg:col-span-8 space-y-4`: 3 static job cards, each `<div class="bg-white p-6 rounded-lg shadow-sm border-l-4 border-primary">` with title + employer + pill badge `text-xs font-bold bg-surface-container px-3 py-1 rounded-full text-primary` (URGENTE/NOVA)
- TODO comment: `// TODO(future): wire to jobs DB model when available.`
- Mirror desktop.html Row 8.

**BusinessDirectory.tsx + BusinessCard.tsx** — REWRITE to match Guia Comercial tile pattern (in case they're still used by other pages or by GuiaComercialSection):
- BusinessCard: simple `<div class="bg-white p-4 rounded-md shadow-sm flex flex-col items-center gap-2">` with icon + label.
- BusinessDirectory: wrapper that maps businesses → BusinessCard in a 2→6 col responsive grid.
- If GuiaComercialSection inlines the tiles directly (as above), these files may become unused. If so, keep them minimal (avoid deletion to prevent import errors in `/categoria/guia-comercial` if it imports them). Grep usage and decide: either rewrite or leave them as thin wrappers around the new tile markup.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && npx tsc --noEmit 2>&1 | grep -E "(GuiaComercialSection|PricingSection|EventsScroller|JobsSection)\\.tsx" ; test $? -ne 0</automated>
  </verify>
  <done>GuiaComercialSection renders a 2→6 col tile grid with bg-surface-container wrapper. PricingSection exports 3 tiers with Prata scaled and R$ 299/699/1999 prices. EventsScroller and JobsSection exist as static placeholders with TODO comments. BusinessDirectory/BusinessCard updated or left as thin wrappers.</done>
</task>

<task type="auto" id="01-03-T4">
  <name>Task 4: Rewrite app/(public)/page.tsx composition + build verification</name>
  <files>app/(public)/page.tsx</files>
  <action>
REWRITE `app/(public)/page.tsx`. Preserve `getHomeData()` server fetch (or whatever the existing fetch function is called — grep to confirm). Only the JSX composition changes.

Target structure:
```tsx
import { getHomeData } from '@/lib/queries' // existing path
import { HeroArticle } from '@/components/news/HeroSection'
import { ArticleCard } from '@/components/news/ArticleCard'
import { Sidebar } from '@/components/layout/Sidebar'
import { MaisLidas } from '@/components/widgets/MaisLidas'
import { GuiaComercialSection } from '@/components/sections/GuiaComercialSection'
import { EventsScroller } from '@/components/sections/EventsScroller'
import { JobsSection } from '@/components/sections/JobsSection'
import { PricingSection } from '@/components/sections/PricingSection'

export default async function HomePage() {
  const { allPosts, categories, sponsoredAd, trendingPosts } = await getHomeData()
  const hero = allPosts[0]
  const gridPosts = allPosts.slice(1, 4)
  const secondaryPosts = allPosts.slice(4, 7)
  const latestPosts = allPosts.slice(7, 12)

  return (
    <>
      {/* Top leaderboard ad — TODO(01-04): replace with <AdSlot format="leaderboard" position="GRID_BANNER_TOP" /> */}
      <div className="bg-surface py-4 flex justify-center">
        <div className="w-[728px] max-w-full h-[90px] border-2 border-dashed border-outline-variant bg-[#EEEEEE] flex items-center justify-center relative">
          <span className="absolute top-1 left-2 text-[8px] font-bold bg-tertiary-fixed text-on-tertiary-fixed px-1 rounded-sm">PUBLICIDADE</span>
          <span className="text-outline text-xs italic font-label">[ 728×90 — Espaço disponível: (45) 99999-9999 ]</span>
        </div>
      </div>

      {/* Main content grid */}
      <div className="max-w-[1200px] mx-auto px-4 py-10 grid grid-cols-1 lg:grid-cols-12 gap-8">
        {/* Main column */}
        <div className="lg:col-span-8 space-y-10">
          {hero && <HeroArticle post={hero} />}

          {/* Desktop 3-col sub-grid */}
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6 hidden md:grid">
            {gridPosts.map(post => (
              <ArticleCard key={post.id} post={post} variant="grid" />
            ))}
          </div>

          {/* Mobile secondary list */}
          <div className="space-y-6 md:hidden">
            {secondaryPosts.map(post => (
              <ArticleCard key={post.id} post={post} variant="secondary" />
            ))}
          </div>

          {/* Inline ad — TODO(01-04): replace with <AdSlot format="inline" position="INLINE_BANNER" /> */}
          <div className="w-full h-[250px] bg-surface-container border border-outline-variant flex items-center justify-center relative">
            <span className="absolute top-1 left-2 text-[8px] font-bold bg-tertiary-fixed text-on-tertiary-fixed px-1 rounded-sm">PUBLICIDADE</span>
            <span className="text-outline text-xs italic font-label">[ 300×250 inline — Espaço disponível ]</span>
          </div>
        </div>

        {/* Sidebar (desktop only) */}
        <Sidebar trendingPosts={trendingPosts} latestPosts={latestPosts} />

        {/* Mobile-only Mais Lidas card after inline ad */}
        <div className="lg:hidden bg-surface-container-lowest rounded-xl p-6 shadow-sm">
          <MaisLidas posts={trendingPosts.slice(0, 3)} />
        </div>
      </div>

      {/* Full-width sections */}
      <GuiaComercialSection />
      <EventsScroller />
      <JobsSection />
      <PricingSection variant="home" />

      {/* Bottom leaderboard ad — TODO(01-04): replace with <AdSlot format="leaderboard" position="GRID_BANNER_BOTTOM" /> */}
      <div className="bg-surface py-12 flex justify-center">
        <div className="w-[728px] max-w-full h-[90px] border-2 border-dashed border-outline-variant bg-[#EEEEEE] flex items-center justify-center relative">
          <span className="absolute top-1 left-2 text-[8px] font-bold bg-tertiary-fixed text-on-tertiary-fixed px-1 rounded-sm">PUBLICIDADE</span>
          <span className="text-outline text-xs italic font-label">[ 728×90 — Espaço disponível ]</span>
        </div>
      </div>
    </>
  )
}
```

Notes:
- All 3 AdSlot TODO comments must match the format plan 01-04 expects so the swap is trivial.
- Remove imports of MainGrid, CategorySection, NewsletterWidget, TrendingWidget (replaced by MaisLidas), HeroSection (now HeroArticle — adjust import name).
- Keep the signature of getHomeData() unchanged. If the current fetch returns fewer/different fields, adapt the destructuring accordingly but do not alter the query.
- `sponsoredAd` from the existing fetch is available — use it in the inline ad placeholder if convenient, or ignore until plan 01-04 refactors all ad rendering.

Run build:
```bash
cd C:/Users/crist/foz-foco-real
export DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder"
npm run build 2>&1 | tee .build.log
```
Expect exit 0. If a deleted component (CategorySection/NewsletterWidget) is still imported elsewhere, remove those imports or stub the component.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder" npm run build 2>&1 | grep -q "Compiled successfully"</automated>
  </verify>
  <done>app/(public)/page.tsx composes Hero + 3-col grid + inline ad placeholder + Sidebar (desktop) + mobile MaisLidas card + Guia + Events + Jobs + Pricing + bottom ad placeholder. Build passes. Mobile stack collapses correctly. All 3 ad placeholders have TODO comments matching plan 01-04's AdSlot interface.</done>
</task>

</tasks>

<files_created>
- components/widgets/UltimasList.tsx
- components/sections/GuiaComercialSection.tsx
- components/sections/PricingSection.tsx
- components/sections/EventsScroller.tsx
- components/sections/JobsSection.tsx
- components/widgets/MaisLidas.tsx (may replace TrendingWidget.tsx via rename)
</files_created>

<files_modified>
- app/(public)/page.tsx (FULL REWRITE of JSX)
- components/news/HeroSection.tsx (FULL REWRITE as HeroArticle)
- components/news/ArticleCard.tsx (SIMPLIFY to 3 variants)
- components/news/CategoryBadge.tsx (adjust default styling)
- components/layout/Sidebar.tsx (REWRITE as 4-block stack)
- components/widgets/TrendingWidget.tsx (RENAME/REWRITE as MaisLidas)
- components/ads/BusinessDirectory.tsx (rewrite to tile pattern or thin wrapper)
- components/ads/BusinessCard.tsx (simplify)
</files_modified>

<files_deleted>
- components/sections/MainGrid.tsx (OR reduced to NewsGrid3 helper)
- components/sections/CategorySection.tsx
- components/widgets/NewsletterWidget.tsx
</files_deleted>

<acceptance_criteria>
1. Vercel preview `/` desktop view: visitor sees leaderboard ad placeholder → 12-col main grid with hero + 3-col news sub-grid + inline ad + sidebar (halfpage ad + Mais Lidas + rectangle ad + Últimas) → full-width Guia Comercial tiles → EventsScroller → JobsSection → Pricing (Prata popular scaled) → bottom leaderboard.
2. Vercel preview `/` mobile view: visitor sees mobile leaderboard placeholder → hero (mobile-b aspect-4/3 or similar) → 3 secondary posts with thumb-right → inline ad → Mais Lidas card → Guia (2-col) → Events (horizontal scroll) → Jobs (stacked) → Pricing (stacked 3 tiers) → bottom ad. Chrome from 01-02 frames everything.
3. Grid cards have NO excerpt and NO byline (image + category eyebrow + Newsreader title only).
4. Mais Lidas widget shows italic "01", "02", "03" Newsreader numbers and `border-b-4 border-primary` heading.
5. Últimas shows HH:MM prefixed links with divide-y separators.
6. Pricing section Prata tier is visually taller (scaled 105%) with MAIS POPULAR badge.
7. Events/Jobs sections render static placeholder content with no crashes; TODO comments visible in source.
8. Dark mode preserves legibility across all sections.
9. `npm run build` exits 0; Vercel preview READY.
</acceptance_criteria>

<verification_steps>
1. Local build with placeholder DATABASE_URL succeeds.
2. Vercel preview READY.
3. Desktop `/`:
   - Inspect 12-col grid layout (Chrome DevTools grid overlay).
   - Confirm hero article, 3 grid cards, inline ad placeholder, sidebar 4 blocks.
   - Confirm Guia Comercial 6-col tiles.
   - Confirm Pricing with Prata scaled.
4. Mobile `/` (375×812):
   - Confirm single column.
   - Confirm sidebar is hidden but Mais Lidas appears as card inline.
   - Confirm Guia is 2-col.
5. Dark mode toggle on `/` — all sections legible.
6. Open any category page (`/categoria/politica`) — must still render (plan 01-01 already swept those, Sidebar and related components there are untouched by this plan unless shared imports broke).
7. Open a post page (`/[slug]`) — must still render with .article-body typography.
</verification_steps>

<success_criteria>
- All 4 tasks green.
- Build passes + Vercel READY.
- Homepage structurally mirrors Stitch desktop.html rows 5–9 and mobile-b sections 3–7.
- Ad placeholders in place with TODO comments ready for plan 01-04 swap.
- Events/Jobs static placeholders present without blocking the build.
- NewsletterWidget and CategorySection removed; MainGrid replaced by inline composition.
</success_criteria>

<commit_message>
feat(01-03): rebuild homepage with Stitch hero + 3-col grid + sidebar widgets + commercial sections

- app/(public)/page.tsx: new 12-col composition (hero + 3-col sub-grid + inline ad + Sidebar desktop / Mais Lidas card mobile + Guia + Events + Jobs + Pricing + bottom ad)
- components/news/HeroSection.tsx: FULL REWRITE as HeroArticle (inline grid hero, gradient overlay, Newsreader title, 'Leia mais →' amber CTA)
- components/news/ArticleCard.tsx: simplify to 3 variants (grid / secondary / compact), drop excerpt and byline on grid cards
- components/news/CategoryBadge.tsx: default to bg-tertiary-fixed amber treatment
- components/layout/Sidebar.tsx: 4-block stack (halfpage ad placeholder / MaisLidas / rectangle ad placeholder / UltimasList), hidden on mobile
- components/widgets/MaisLidas.tsx: NEW — replaces TrendingWidget, italic Newsreader numbers + border-b-4 border-primary heading
- components/widgets/UltimasList.tsx: NEW — HH:MM prefixed divide-y list
- components/sections/GuiaComercialSection.tsx: NEW — 2→6 col icon tile grid with bg-surface-container wrapper
- components/sections/PricingSection.tsx: NEW — 3 tiers on navy gradient, Prata scaled + MAIS POPULAR badge, R$ 299/699/1999
- components/sections/EventsScroller.tsx: NEW static placeholder with TODO for future data wiring
- components/sections/JobsSection.tsx: NEW static placeholder with TODO
- components/sections/MainGrid.tsx, CategorySection.tsx, widgets/NewsletterWidget.tsx: DELETED

Ad slots marked with TODO(01-04) comments for plan 01-04 AdSlot swap. Backend untouched, getHomeData unchanged.
</commit_message>

<dependencies>
- Depends on: 01-01 (tokens), 01-02 (chrome composition provides shell)
- Blocks: 01-04 (AdSlot component replaces the inline placeholders seeded here)
</dependencies>

<output>
After completion, create `.planning/phases/01-redesign-ui-completo/01-03-SUMMARY.md` documenting:
- Final JSX composition of app/(public)/page.tsx
- List of deleted legacy components
- TODO markers left for plan 01-04 (exact line numbers)
- Pricing tier values used
- Mobile stacking order
</output>
