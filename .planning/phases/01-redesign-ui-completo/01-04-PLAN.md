---
phase: 01-redesign-ui-completo
plan: 01-04
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - components/ads/AdSlot.tsx
  - components/ads/AdBannerTop.tsx
  - components/ads/AdBannerBottom.tsx
  - components/ads/AdInlineBanner.tsx
  - components/ads/AdSidebarSticky.tsx
  - components/ads/AdSectionSponsor.tsx
  - components/ads/AdSponsoredCard.tsx
  - app/(public)/page.tsx
  - app/(public)/anunciantes/page.tsx
  - components/layout/Sidebar.tsx
autonomous: true
requirements: [ADS-01, ADS-02, ADS-03, ANUN-01, ANUN-02, ANUN-03, ANUN-04, ANUN-05, ANUN-06, ANUN-07, BUILD-01, BUILD-02, BUILD-03]
goal: "Ship the unified <AdSlot> component (5 IAB formats with placeholder fallback), replace all ad placeholders seeded in plan 01-03 with real AdSlot usage, and rewrite /anunciantes landing with the new design system."
must_haves:
  truths:
    - "A single <AdSlot format=... position=... /> component renders all IAB formats (leaderboard 728×90, rectangle 300×250, halfpage 300×600, mobileBanner 320×50 / h-24, inline full-width h-250) and fetches the corresponding ad from /api/ads?position= preserving existing impression + click tracking endpoints."
    - "When no ad is returned for a position, AdSlot renders a dashed placeholder matching the Stitch look (PUBLICIDADE label + [ WxH — Espaço disponível: (45) 99999-9999 ] text) — never a broken image."
    - "Homepage placeholders seeded by plan 01-03 (top leaderboard, inline 300×250, sidebar halfpage, sidebar rectangle, bottom leaderboard) are replaced by real <AdSlot> usages."
    - "Legacy ad components (AdBannerTop, AdBannerBottom, AdInlineBanner, AdSidebarSticky) are either deleted or refactored into thin wrappers around AdSlot — no duplicated fetch/tracking logic."
    - "/anunciantes landing mirrors the new design system end-to-end: hero editorial with Newsreader headline + badge + dual CTAs → stats bar (4 metrics) → 3-tier pricing (reuses <PricingSection variant='landing'>) → 6 ad format tiles → testimonials → final CTA in brand gradient."
    - "/anunciantes does NOT show ad placeholders — it IS the ad sales page."
    - "npm run build exits 0. Vercel preview READY. /admin/* pages remain untouched and functional."
  artifacts:
    - path: "components/ads/AdSlot.tsx"
      provides: "Unified IAB slot component with 5 formats + placeholder fallback + impression/click tracking"
      contains: "format"
    - path: "app/(public)/anunciantes/page.tsx"
      provides: "Rebuilt landing page (hero / stats / pricing / formats / testimonials / final CTA)"
      contains: "PricingSection"
    - path: "app/(public)/page.tsx"
      provides: "Homepage with AdSlot swapped in for placeholders"
      contains: "AdSlot"
  key_links:
    - from: "components/ads/AdSlot.tsx"
      to: "/api/ads endpoints"
      via: "fetch by position, tracking impression and click"
      pattern: "/api/ads"
    - from: "app/(public)/page.tsx"
      to: "components/ads/AdSlot.tsx"
      via: "5 AdSlot instances at top leaderboard, inline, sidebar halfpage, sidebar rectangle, bottom leaderboard"
      pattern: "<AdSlot"
    - from: "app/(public)/anunciantes/page.tsx"
      to: "components/sections/PricingSection.tsx"
      via: "variant='landing' reuse"
      pattern: "PricingSection"
---

<objective>
Close phase 1 by (a) delivering a single unified `<AdSlot>` that replaces 5 fragmented ad components, (b) wiring it into every homepage ad placeholder seeded by plan 01-03, and (c) rewriting `/anunciantes` to align visually with the rest of the redesigned portal and to reuse the `<PricingSection>` component from plan 01-03.

This is the last plan in phase 1. When it ships, every requirement in ROADMAP phase 1 is complete and the site's public UI is a 1:1 reproduction of the Stitch contract.
</objective>

<context_summary>
- RESEARCH §1.7 lists all ad slot dimensions and placements across the 4 Stitch screens.
- RESEARCH §2.1 Ads section inventories the 5 existing ad components that AdSlot unifies.
- RESEARCH §4 Plan 01-04 gives the AdSlot signature and placeholder format verbatim.
- RESEARCH §5 Q1 flags an open question about the /api/ads response shape — executor must resolve by reading the current fetch logic in AdBannerTop.tsx.
- Plan 01-03 seeded 5 placeholder divs with TODO(01-04) comments ready for swap.
- CONTEXT.md locks backend untouched: no schema changes, no new API routes. AdSlot must reuse existing `/api/ads`, `/api/ads/:id/impression`, `/api/ads/:id/click` endpoints.
</context_summary>

<pre_flight_reads>
Executor MUST read these files in full before any edit:
1. C:\Users\crist\foz-foco-real\.planning\phases\01-redesign-ui-completo\01-RESEARCH.md (sections 1.1 rows 3/5/10, 1.5 slot dimensions, 1.7, 2.1 Ads, 2.2 AdPosition enum, 4 Plan 01-04, 5 Q1)
2. C:\Users\crist\foz-foco-real\.planning\research\stitch\desktop.html (Row 3 leaderboard, Row 5 sidebar 300×600 + 300×250 + inline, Row 10 footer banner)
3. C:\Users\crist\foz-foco-real\components\ads\AdBannerTop.tsx (current fetch + impression + click logic — the source of truth to migrate into AdSlot)
4. C:\Users\crist\foz-foco-real\components\ads\AdBannerBottom.tsx
5. C:\Users\crist\foz-foco-real\components\ads\AdInlineBanner.tsx
6. C:\Users\crist\foz-foco-real\components\ads\AdSidebarSticky.tsx
7. C:\Users\crist\foz-foco-real\components\ads\AdSectionSponsor.tsx
8. C:\Users\crist\foz-foco-real\components\ads\AdSponsoredCard.tsx
9. C:\Users\crist\foz-foco-real\app\(public)\anunciantes\page.tsx (full — the current structure is roughly right; this rewrite is styling-first)
10. C:\Users\crist\foz-foco-real\app\(public)\page.tsx (post 01-03 — contains the TODO placeholders to replace)
11. C:\Users\crist\foz-foco-real\components\layout\Sidebar.tsx (post 01-03 — contains 2 TODO placeholders for halfpage and rectangle)
12. C:\Users\crist\foz-foco-real\components\sections\PricingSection.tsx (to pass `variant='landing'` from /anunciantes)
13. C:\Users\crist\foz-foco-real\types\index.ts (confirm Ad type + AdPosition enum)
14. Any existing `/api/ads` route file — grep `app/api/ads` for route.ts to confirm response shape (array vs single ad)
</pre_flight_reads>

<interfaces>
Key types and contracts executor needs (extracted from types/index.ts — confirm during pre-flight read):

```ts
// types/index.ts (expected shape)
export type Ad = {
  id: string
  title: string
  type: string
  imageUrl: string
  targetUrl: string
  client: string
  position: AdPosition
  frequency: number
  impressions: number
  clicks: number
  active: boolean
  startsAt: Date | string
  endsAt: Date | string
}

export enum AdPosition {
  GRID_BANNER = 'GRID_BANNER',
  GRID_BANNER_TOP = 'GRID_BANNER_TOP',
  GRID_BANNER_BOTTOM = 'GRID_BANNER_BOTTOM',
  SIDEBAR = 'SIDEBAR',
  INLINE_BANNER = 'INLINE_BANNER',
  SECTION_SPONSOR = 'SECTION_SPONSOR',
  BREAKING_BANNER = 'BREAKING_BANNER',
  POST_DETAIL = 'POST_DETAIL',
  ARTICLE_TOP = 'ARTICLE_TOP',
  FEED_BETWEEN = 'FEED_BETWEEN',
  FEED_TOP = 'FEED_TOP',
}
```

AdSlot target API:
```ts
type AdFormat = 'leaderboard' | 'rectangle' | 'halfpage' | 'mobileBanner' | 'inline'

type AdSlotProps = {
  format: AdFormat
  position: AdPosition
  className?: string
  placeholder?: { phone?: string; message?: string }
}
```
</interfaces>

<tasks>

<task type="auto" id="01-04-T1">
  <name>Task 1: Create the unified AdSlot component</name>
  <files>components/ads/AdSlot.tsx</files>
  <action>
Create `components/ads/AdSlot.tsx` as a CLIENT component (it fetches and tracks impressions/clicks from the browser).

Signature:
```ts
'use client'
import { useEffect, useState } from 'react'
import Image from 'next/image'
import type { Ad, AdPosition } from '@/types'

type AdFormat = 'leaderboard' | 'rectangle' | 'halfpage' | 'mobileBanner' | 'inline'

type AdSlotProps = {
  format: AdFormat
  position: AdPosition
  className?: string
  placeholder?: { phone?: string; message?: string }
}

export function AdSlot({ format, position, className, placeholder }: AdSlotProps) { ... }
```

Dimension table (apply to inner box):
- `leaderboard`: `w-[728px] max-w-full h-[90px]` (desktop 728×90)
- `rectangle`: `w-[300px] h-[250px] mx-auto`
- `halfpage`: `w-[300px] h-[600px] mx-auto`
- `mobileBanner`: `w-full h-[90px]`
- `inline`: `w-full h-[250px]`

Fetch logic (migrate from AdBannerTop.tsx — do not reinvent):
1. On mount, `fetch(`/api/ads?position=${position}`)` → set local state `ad: Ad | null`.
2. If the endpoint returns an array, pick index 0 (or use `frequency` weighting if the current code does it — preserve whatever AdBannerTop does). If it returns a single object, use it directly.
3. If `ad` is set, fire `fetch(`/api/ads/${ad.id}/impression`, { method: 'POST' })` once per mount (guard with a ref to avoid double-firing in StrictMode).
4. On click, fire `fetch(`/api/ads/${ad.id}/click`, { method: 'POST' })` before navigation (use `navigator.sendBeacon` if available for reliability).

Render logic:
- If `ad`: render `<a href={ad.targetUrl} target="_blank" rel="noopener sponsored" onClick={trackClick}>` wrapping `<Image src={ad.imageUrl} alt={ad.title} fill class="object-cover" />` inside a positioned container matching the dimension table above.
- If no `ad` (loading or empty): render placeholder:
  ```tsx
  <div class="relative border-2 border-dashed border-outline-variant bg-surface-container flex items-center justify-center {dimensionClasses}">
    <span class="absolute top-1 left-2 text-[8px] font-bold bg-tertiary-fixed text-on-tertiary-fixed px-1 rounded-sm">PUBLICIDADE</span>
    <span class="text-outline text-xs italic font-label">[ {W}×{H} — Espaço disponível: {placeholder?.phone ?? '(45) 99999-9999'} ]</span>
  </div>
  ```
- Outer wrapper: `<div class={`flex justify-center ${className ?? ''}`}>`

The component must never throw. Wrap the fetch in try/catch and fall back to placeholder on error.

Also export a small sub-component `<AdSlotPlaceholder format position />` that renders only the placeholder (no fetch) — useful for server-side rendering paths or during skeletons. Keep the main `<AdSlot>` client-side.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && npx tsc --noEmit 2>&1 | grep -E "AdSlot\\.tsx" ; test $? -ne 0</automated>
  </verify>
  <done>components/ads/AdSlot.tsx exports AdSlot + AdSlotPlaceholder. 5 formats render correct dimensions. Fetch, impression tracking, click tracking all preserved from AdBannerTop. Placeholder renders on empty/error. No TS errors.</done>
</task>

<task type="auto" id="01-04-T2">
  <name>Task 2: Refactor legacy ad components into thin wrappers (or delete)</name>
  <files>components/ads/AdBannerTop.tsx, components/ads/AdBannerBottom.tsx, components/ads/AdInlineBanner.tsx, components/ads/AdSidebarSticky.tsx, components/ads/AdSectionSponsor.tsx, components/ads/AdSponsoredCard.tsx</files>
  <action>
For each legacy ad component, grep its usages across the repo (`grep -r "from '@/components/ads/AdBannerTop'"` etc.). Decision tree:

1. **If zero usages**: DELETE the file.
2. **If used only by files rewritten in plans 01-02/01-03**: DELETE and remove the import.
3. **If used by untouched files (e.g., `/categoria/[slug]`, `/[slug]`, `/busca`)**: refactor to a one-line wrapper around AdSlot:
   ```tsx
   // components/ads/AdBannerTop.tsx
   import { AdSlot } from './AdSlot'
   import { AdPosition } from '@/types'
   export function AdBannerTop() {
     return <AdSlot format="leaderboard" position={AdPosition.GRID_BANNER_TOP} />
   }
   ```

Specific per-file guidance:
- **AdBannerTop**: wrapper → `<AdSlot format="leaderboard" position={AdPosition.GRID_BANNER_TOP} />`
- **AdBannerBottom**: wrapper → `<AdSlot format="leaderboard" position={AdPosition.GRID_BANNER_BOTTOM} />`
- **AdInlineBanner**: wrapper → `<AdSlot format="inline" position={AdPosition.INLINE_BANNER} />`
- **AdSidebarSticky**: split into two wrappers OR change signature to take a `format` prop. Because Sidebar (plan 01-03) uses both halfpage and rectangle, either (a) export two named components `AdSidebarHalfpage` / `AdSidebarRectangle`, or (b) Sidebar imports AdSlot directly. Preference: **Sidebar imports AdSlot directly** — delete AdSidebarSticky unless a non-home page imports it.
- **AdSectionSponsor**: repurpose as `<PatrocinadoCard>` matching mobile-b section 5 (border-l-4 border-tertiary card). This is a manual rebuild; it's a distinct visual from AdSlot (it's a styled sponsored card, not a banner). Keep if used anywhere; otherwise delete.
- **AdSponsoredCard**: DELETE unless grep shows an import.

Goal: by end of task, only ONE component (`AdSlot`) contains fetch/tracking logic. Other files are either deleted or one-line wrappers.

After refactoring, run build to confirm no broken imports.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder" npm run build 2>&1 | grep -q "Compiled successfully"</automated>
  </verify>
  <done>Only AdSlot contains fetch+tracking logic. All legacy ad components are either deleted or reduced to thin wrappers. Build passes. No orphan imports anywhere in the repo.</done>
</task>

<task type="auto" id="01-04-T3">
  <name>Task 3: Swap homepage + Sidebar placeholders with real AdSlot usage</name>
  <files>app/(public)/page.tsx, components/layout/Sidebar.tsx</files>
  <action>
Edit `app/(public)/page.tsx` to replace the 3 placeholder divs seeded in plan 01-03 with real `<AdSlot>` instances. The TODO(01-04) comments mark the exact swap sites:

1. Top leaderboard (before main grid):
   ```tsx
   <AdSlot format="leaderboard" position={AdPosition.GRID_BANNER_TOP} className="bg-surface py-4" />
   ```
2. Inline 300×250 (inside main column after sub-grid):
   ```tsx
   <AdSlot format="inline" position={AdPosition.INLINE_BANNER} />
   ```
3. Bottom leaderboard (after PricingSection):
   ```tsx
   <AdSlot format="leaderboard" position={AdPosition.GRID_BANNER_BOTTOM} className="bg-surface py-12" />
   ```

Import `AdSlot` and `AdPosition` at the top of the file. Remove the inline placeholder divs entirely.

Edit `components/layout/Sidebar.tsx` to replace its 2 placeholder divs with:
1. `<AdSlot format="halfpage" position={AdPosition.SIDEBAR} />` (first block)
2. `<AdSlot format="rectangle" position={AdPosition.SIDEBAR} />` (third block, between MaisLidas and UltimasList)

Note: both use `AdPosition.SIDEBAR` because the enum doesn't distinguish halfpage vs rectangle. If the `/api/ads?position=SIDEBAR` endpoint returns multiple ads, AdSlot's fetch logic should pick by index (halfpage gets [0], rectangle gets [1]) or by a secondary filter. For now, accept that both may show the same ad — this is a backend constraint and resolving it is out of scope (no new enum values allowed).

Mobile stacking: keep the Sidebar `hidden lg:block` wrapper so ads don't double-render on mobile (mobile has its own inline ad already in the main column).

Verify no placeholder divs with `PUBLICIDADE` text remain in these two files.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && grep -E "TODO\\(01-04\\)" app/(public)/page.tsx components/layout/Sidebar.tsx ; test $? -ne 0</automated>
  </verify>
  <done>Homepage and Sidebar use AdSlot for all 5 ad positions. All TODO(01-04) comments removed. Build passes. Empty slots render the unified placeholder; filled slots render real ads with tracking.</done>
</task>

<task type="auto" id="01-04-T4">
  <name>Task 4: Rewrite /anunciantes landing page with the new design system</name>
  <files>app/(public)/anunciantes/page.tsx</files>
  <action>
REWRITE `app/(public)/anunciantes/page.tsx` (server component — keep the file as a server component; no client interactivity needed beyond links). Preserve the overall structure (hero / stats / pricing / formats / testimonials / final CTA) but restyle everything with the new tokens and Tailwind utilities. Remove all inline `style={{}}` blocks and legacy class names.

Target structure:

```tsx
import { PricingSection } from '@/components/sections/PricingSection'
import { Users, Eye, MapPin, TrendingUp, Megaphone, Newspaper, Monitor, Smartphone, Globe, Mail } from 'lucide-react'

export default function AnunciantesPage() {
  return (
    <>
      {/* 1. Hero editorial */}
      <section className="bg-surface py-20 px-4">
        <div className="max-w-[1200px] mx-auto text-center">
          <span className="inline-block bg-tertiary-fixed text-on-tertiary-fixed text-[10px] font-bold tracking-widest uppercase px-3 py-1 rounded-sm">
            Para Anunciantes
          </span>
          <h1 className="mt-6 font-headline text-5xl md:text-6xl font-bold text-on-surface leading-tight">
            Alcance quem <span className="italic">decide</span> em Foz do Iguaçu
          </h1>
          <p className="mt-6 text-lg text-outline-variant max-w-2xl mx-auto font-body">
            O portal de notícias mais lido de Foz do Iguaçu coloca sua marca na frente de mais de 50 mil leitores locais todos os meses.
          </p>
          <div className="mt-10 flex flex-col sm:flex-row gap-4 justify-center">
            <a href="https://wa.me/5545999999999" className="bg-primary text-white px-8 py-4 rounded-md font-bold text-xs uppercase tracking-widest hover:bg-primary-container transition-colors">
              Falar com um consultor
            </a>
            <a href="#planos" className="border-2 border-primary text-primary px-8 py-4 rounded-md font-bold text-xs uppercase tracking-widest hover:bg-surface-container transition-colors">
              Ver planos
            </a>
          </div>
        </div>
      </section>

      {/* 2. Stats bar */}
      <section className="bg-on-surface text-surface py-12 px-4">
        <div className="max-w-[1200px] mx-auto grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
          <div>
            <Users className="w-8 h-8 mx-auto text-tertiary-fixed" />
            <p className="mt-3 text-3xl font-headline font-black">50k+</p>
            <p className="text-[10px] uppercase tracking-widest text-outline-variant mt-1">Leitores únicos/mês</p>
          </div>
          <div>
            <Eye className="w-8 h-8 mx-auto text-tertiary-fixed" />
            <p className="mt-3 text-3xl font-headline font-black">180k+</p>
            <p className="text-[10px] uppercase tracking-widest text-outline-variant mt-1">Visualizações/mês</p>
          </div>
          <div>
            <MapPin className="w-8 h-8 mx-auto text-tertiary-fixed" />
            <p className="mt-3 text-3xl font-headline font-black">87%</p>
            <p className="text-[10px] uppercase tracking-widest text-outline-variant mt-1">Audiência local</p>
          </div>
          <div>
            <TrendingUp className="w-8 h-8 mx-auto text-tertiary-fixed" />
            <p className="mt-3 text-3xl font-headline font-black">4:30</p>
            <p className="text-[10px] uppercase tracking-widest text-outline-variant mt-1">Tempo médio</p>
          </div>
        </div>
      </section>

      {/* 3. Pricing — reuse PricingSection from plan 01-03 */}
      <div id="planos">
        <PricingSection variant="landing" />
      </div>

      {/* 4. 6 ad format tiles */}
      <section className="py-20 bg-surface-container-low px-4">
        <div className="max-w-[1200px] mx-auto">
          <h2 className="text-3xl md:text-4xl font-headline font-bold text-center text-on-surface">Formatos disponíveis</h2>
          <p className="text-center text-outline mt-3 max-w-xl mx-auto">Escolha o formato que melhor se encaixa ao seu objetivo.</p>
          <div className="mt-12 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {/* 6 tiles: Leaderboard 728x90, Retângulo 300x250, Half-page 300x600, Mobile banner 320x50, Card patrocinado, Post patrocinado */}
            {/* Each tile: bg-white rounded-lg p-6 shadow-sm with icon + title + dimensions + bullet benefits */}
            {/* Icons: Monitor, Megaphone, Smartphone, Globe, Newspaper, Mail */}
          </div>
        </div>
      </section>

      {/* 5. Testimonials */}
      <section className="py-20 bg-surface px-4">
        <div className="max-w-[1200px] mx-auto">
          <h2 className="text-3xl md:text-4xl font-headline font-bold text-center">Quem confia no Foz em Foco</h2>
          <div className="mt-12 grid grid-cols-1 md:grid-cols-3 gap-6">
            {/* 3 testimonial cards: bg-surface-container-lowest rounded-xl p-6 border-l-4 border-tertiary */}
            {/* Each: quote italic text-on-surface + author name + company */}
          </div>
        </div>
      </section>

      {/* 6. Final CTA in brand gradient */}
      <section className="py-24 px-4 bg-gradient-to-br from-primary via-primary-container to-on-surface text-white text-center">
        <div className="max-w-[800px] mx-auto">
          <h2 className="text-4xl md:text-5xl font-headline font-bold">Pronto para anunciar?</h2>
          <p className="mt-4 text-lg text-on-primary-container">Fale com um consultor e montamos o plano ideal para seu negócio.</p>
          <a href="https://wa.me/5545999999999" className="mt-8 inline-block bg-tertiary-fixed text-on-tertiary-fixed px-10 py-4 rounded-md font-bold text-xs uppercase tracking-widest hover:bg-white transition-colors">
            Começar agora
          </a>
        </div>
      </section>
    </>
  )
}
```

Fill in the 6 ad format tiles and 3 testimonial cards with the existing copy from the current `/anunciantes/page.tsx` (preserve real business data — stats numbers, testimonial names, pricing values). If the current file has more detailed copy, keep it; the above is the structural shell.

Remove all inline `style={{}}` usage. Remove any legacy class references. Do NOT add ad placeholders to this page — per RESEARCH §4 Plan 01-04 key decision: "/anunciantes does NOT show ad placeholders (it IS the ad sales page)."

Alignment: colors, fonts, spacing must match plans 01-01..01-03. PricingSection is reused with `variant="landing"`.

Run final build:
```bash
cd C:/Users/crist/foz-foco-real
export DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder"
npm run build 2>&1 | tee .build.log
```
Expect exit 0.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder" npm run build 2>&1 | grep -q "Compiled successfully" && grep -E "style=\\{\\{" app/(public)/anunciantes/page.tsx ; test $? -ne 0</automated>
  </verify>
  <done>/anunciantes renders 6 sections in order (hero / stats / pricing / formats / testimonials / final CTA), reuses PricingSection with variant='landing', no inline styles, no legacy classes, no ad placeholders. Build passes.</done>
</task>

<task type="auto" id="01-04-T5">
  <name>Task 5: End-to-end build + regression sweep + admin untouched confirmation</name>
  <files>(verification only)</files>
  <action>
Final phase-1 sanity check.

1. Run full build:
   ```bash
   cd C:/Users/crist/foz-foco-real
   export DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder"
   npm run build 2>&1 | tee .build.log
   ```
   Must exit 0 with "Compiled successfully".

2. Grep for any orphan references to deleted/renamed things:
   ```bash
   grep -rE "(TrendingWidget|NewsletterWidget|MainGrid|CategorySection|hero-section|article-card|container-editorial|font-sora|font-bebas|breaking-banner)" app/ components/ 2>&1
   ```
   Expected: zero hits, OR only hits inside files from plan 01-01 sweep that were intentionally left.

3. Confirm `/admin/*` is untouched. Grep for any modifications inside `app/admin/`:
   ```bash
   git diff --name-only main -- app/admin/
   ```
   Expected: zero files. Phase 1 scope explicitly excludes admin.

4. Confirm the Prisma schema is untouched:
   ```bash
   git diff --name-only main -- prisma/
   ```
   Expected: zero files.

5. Deploy to Vercel preview (push the branch). Wait for READY status. Smoke-test:
   - `/` desktop + mobile
   - `/anunciantes` desktop + mobile
   - `/[slug]` (any post) — post detail still renders with .article-body
   - `/categoria/[slug]` — category list still renders
   - `/busca?q=foz` — search still renders
   - Dark mode toggle on each
   - Any `/admin/*` page (login or dashboard) — must still be fully functional visually (no regression)

6. Record the Vercel preview URL in the plan summary.
  </action>
  <verify>
    <automated>cd C:/Users/crist/foz-foco-real && DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder" npm run build 2>&1 | grep -q "Compiled successfully"</automated>
  </verify>
  <done>Build exits 0. No orphan references. No files changed under app/admin/ or prisma/. Vercel preview READY. All 6 public routes render without regression. Dark mode works on all. Admin pages unchanged and functional. Phase 1 complete.</done>
</task>

</tasks>

<files_created>
- components/ads/AdSlot.tsx
</files_created>

<files_modified>
- components/ads/AdBannerTop.tsx (thin wrapper OR deleted)
- components/ads/AdBannerBottom.tsx (thin wrapper OR deleted)
- components/ads/AdInlineBanner.tsx (thin wrapper OR deleted)
- components/ads/AdSidebarSticky.tsx (thin wrapper OR deleted)
- components/ads/AdSectionSponsor.tsx (repurposed or deleted)
- components/ads/AdSponsoredCard.tsx (deleted if unused)
- app/(public)/page.tsx (swap TODO placeholders → AdSlot instances)
- components/layout/Sidebar.tsx (swap TODO placeholders → AdSlot instances)
- app/(public)/anunciantes/page.tsx (FULL RESTYLE — structure preserved)
</files_modified>

<files_deleted>
- Potentially all of: components/ads/AdBannerTop.tsx, AdBannerBottom.tsx, AdInlineBanner.tsx, AdSidebarSticky.tsx, AdSectionSponsor.tsx, AdSponsoredCard.tsx (decision per task 2 grep results)
</files_deleted>

<acceptance_criteria>
1. Vercel preview shows `/` with 5 ad positions rendering via AdSlot: top leaderboard, inline 300×250 in main column, halfpage + rectangle in sidebar, bottom leaderboard.
2. Empty ad positions render the dashed PUBLICIDADE placeholder (not broken images, not blank space).
3. Filled ad positions render the real ad image, track impressions via `/api/ads/:id/impression`, and track clicks via `/api/ads/:id/click`.
4. `/anunciantes` renders 6 sections with the new design system (hero editorial / stats / PricingSection with variant='landing' / 6 format tiles / 3 testimonials / final CTA in brand gradient).
5. Visual coherence: typography (Newsreader headlines + Plus Jakarta Sans body), palette (primary navy / amber CTAs / red pills), spacing consistent between `/` and `/anunciantes`.
6. No inline `style={{}}` in `/anunciantes/page.tsx` or AdSlot.
7. `/admin/*` unchanged (git diff confirms zero files touched under app/admin).
8. `prisma/` unchanged (zero files touched).
9. `npm run build` exits 0. Vercel preview READY.
10. Dark mode legible across homepage, /anunciantes, post detail, category, search.
11. All 30 requirements from REQUIREMENTS.md phase 1 table are satisfied.
</acceptance_criteria>

<verification_steps>
1. Local build with placeholder DATABASE_URL — must succeed.
2. Orphan-reference grep returns zero hits (or only intentional residue).
3. `git diff --name-only main -- app/admin/ prisma/` returns empty.
4. Push to Vercel preview → READY.
5. Smoke-test public routes in preview: `/`, `/anunciantes`, `/[slug]/any-post`, `/categoria/politica` (or equivalent), `/busca?q=foz`.
6. Inspect Network tab on `/`: confirm one `/api/ads?position=GRID_BANNER_TOP` call, one `/api/ads/:id/impression` call per rendered ad.
7. Click a rendered ad: confirm `/api/ads/:id/click` POST fires.
8. Toggle dark mode on all 5 public routes — no contrast failures.
9. Open `/admin` (or `/admin/dashboard`) — confirm no visual regression vs production baseline.
10. Compare `/` and `/anunciantes` side-by-side in the browser — confirm typography, colors, spacing are consistent.
</verification_steps>

<success_criteria>
- All 5 tasks green.
- Build + Vercel preview READY.
- 30/30 phase 1 requirements satisfied.
- AdSlot is the single source of truth for ad rendering/tracking.
- /anunciantes matches the new design system.
- Admin + Prisma confirmed untouched.
- Phase 1 "Redesign UI completo" closes.
</success_criteria>

<commit_message>
feat(01-04): unify ads under <AdSlot>, wire homepage slots, rebuild /anunciantes landing

- components/ads/AdSlot.tsx: NEW unified IAB slot component — 5 formats (leaderboard/rectangle/halfpage/mobileBanner/inline), fetches from /api/ads?position=, tracks impressions + clicks, dashed PUBLICIDADE placeholder fallback
- components/ads/AdBannerTop/Bottom/Inline/SidebarSticky/SectionSponsor/SponsoredCard: refactored to thin wrappers around AdSlot OR deleted when unused
- app/(public)/page.tsx: swap 3 TODO placeholders for real <AdSlot> (GRID_BANNER_TOP leaderboard, INLINE_BANNER inline, GRID_BANNER_BOTTOM leaderboard)
- components/layout/Sidebar.tsx: swap 2 TODO placeholders for <AdSlot format='halfpage'> and <AdSlot format='rectangle'> (both SIDEBAR position)
- app/(public)/anunciantes/page.tsx: FULL RESTYLE — hero editorial + stats bar + PricingSection variant='landing' (reused from 01-03) + 6 ad format tiles + testimonials + final CTA brand gradient. Zero inline styles.

Backend untouched (no Prisma/Supabase/API changes). /admin/* untouched. Build passes. Closes phase 1.
</commit_message>

<dependencies>
- Depends on: 01-01 (tokens), 01-02 (chrome), 01-03 (homepage placeholders + PricingSection)
- Blocks: nothing (final plan of phase 1)
</dependencies>

<output>
After completion, create `.planning/phases/01-redesign-ui-completo/01-04-SUMMARY.md` documenting:
- Final AdSlot API + fetch/tracking behavior
- List of legacy ad components deleted vs refactored
- /anunciantes final section order and copy sources
- Vercel preview URL
- Full requirements checklist (30 items) with satisfied status
- Phase 1 close-out notes
</output>
